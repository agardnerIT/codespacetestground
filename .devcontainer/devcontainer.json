// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: https://github.com/devcontainers/templates/tree/main/src/kubernetes-helm
{
	"name": "Codespaces Testing Ground",

	"hostRequirements": {
		"cpus": 2
	},

	// Use 'postStartCommand' to run commands after the container starts.
    "postStartCommand": "python app.py"

	// // Add recommended secrets
    /*
	"secrets": {
	 	  "DT_URL": {
	 		"description": "eg. https://abc12345.live.dynatrace.com"
		},
		  "DT_OPERATOR_TOKEN": {
	    	        "description": "Dynatrace API token with these permissions: Create ActiveGate token, Read entities, Read Settings, Write Settings, Access problem and event feed, metrics and topology, Read configuration, Write configuration, Paas integration - installer downloader"
		},
		  "DT_TOKEN": {
	    	        "description": "Dynatrace API token with these permissions: Ingest metrics, Ingest logs, Ingest events, Ingest OpenTelemetry, Read metrics"
		},
  		  "DT_WRITE_SETTINGS_TOKEN": {
			       "description": "Dynatrace API token with these permissions: Write Settings"
        },
		  "OPEN_AI_TOKEN": {
			"description": "OpenAI API Token. Go to https://platform.openai.com/api-keys"
		}
	}
    */
}
